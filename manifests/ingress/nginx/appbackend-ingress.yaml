apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: appbackend-ingress
  namespace: test
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  rules:
  - host: appbackend.dmhub.cn 
    http:
      paths:
      - path: /
        backend:
          serviceName: appbackend
          servicePort: 18011

